version: "3.8"

services:
  bot:
    image: ghcr.io/gorlix/morgencalendar-telegram-bot:latest
    container_name: morgen_bot
    restart: unless-stopped
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
    volumes:
      - ./data:/app/data
